import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Test Project One',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
